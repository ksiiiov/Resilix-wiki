---
label: Установка на Linux
---

# 🐧 Установка VPN ShadowSocks на Linux

Здесь — базовая схема подключения **OKVpn** на Linux через **ShadowSocks**.

---

## 1. Получить ключ ShadowSocks

1. Войдите в личный кабинет или бота OKVpn.
2. Откройте раздел **ShadowSocks**.
3. Скопируйте строку `ss://...` или сохраните файл конфигурации.

---

## 2. Выбрать клиент под Linux

Может быть:

- графический клиент ShadowSocks (GUI);
- консольный клиент `ss-local` и т.п.

Ориентируйтесь на рекомендации на [`https://okvpn.wiki/`](https://okvpn.wiki/) и под ваш дистрибутив.

---

### Вариант A. Графический клиент

1. Установите GUI‑клиент ShadowSocks (через репозиторий, Flatpak, AppImage и т.д.).
2. Запустите приложение.
3. Добавьте новый профиль:
   - вставьте строку `ss://...`;
   - или импортируйте файл конфигурации.
4. Сохраните профиль.
5. Нажмите **Connect / Подключить**.

---

### Вариант B. Консольный клиент

1. Установите `shadowsocks-libev` или аналогичный пакет:

```bash
sudo apt install shadowsocks-libev
```

2. Сформируйте `config.json` на основе данных, которые даёт OKVpn (порт, пароль, метод шифрования и т.п.), либо используйте готовый шаблон из их документации.
3. Запустите клиент, например:

```bash
ss-local -c config.json
```

4. В системе настройте использование локального SOCKS/HTTP‑прокси (через настройки сети или браузера).

---

## 3. Проверка

1. Откройте браузер.
2. Зайдите на сайт проверки IP и убедитесь, что IP изменился.
3. Проверьте доступ к ранее заблокированным ресурсам.

---

## 4. Если не работает

1. Проверьте логи клиента на наличие ошибок.
2. Убедитесь, что время и дата в системе установлены правильно.
3. Попробуйте другой сервер/страну в личном кабинете OKVpn и сгенерируйте новый ключ.
4. Временно отключите файрволы/фильтры, которые могут резать трафик.

Если разобраться не получается — напишите в поддержку **@okvpn_support_bot**, приложите:

- название дистрибутива;
- версию клиента;
- выдержку из лога с ошибкой (без чувствительных данных).

---

[!button text="⬅️ Назад к ShadowSocks" variant="link"](../)
