---
label: Установка на Linux
---

# 🐧 Установка VPN VLESS на Linux

Здесь описана базовая схема подключения **OKVpn** на Linux через протокол **VLESS**.

---

## 1. Данные для подключения

Как и на других платформах, сначала получите конфигурацию:

1. Зайдите в личный кабинет или бота OKVpn.
2. Откройте раздел **VLESS**.
3. Скопируйте ссылку `vless://...` или скачайте файл конфигурации.

---

## 2. Клиенты VLESS под Linux

Есть два основных подхода:

- **Графический клиент** (например, Qv2ray, NekoRay и др.).
- **Консольный запуск** через `xray/v2ray` с конфигом.

Ориентируйтесь на то, что рекомендует OKVpn в официальной инструкции (`https://okvpn.wiki/`).

---

### Вариант A. Графический клиент

1. Установите Qv2ray/NekoRay из пакета вашей системы, AppImage, Flatpak или .deb/.rpm (в зависимости от дистрибутива).
2. Запустите приложение.
3. Найдите функцию **Import / Импорт**:
   - импорт из буфера (`vless://...`);
   - или импорт из файла.
4. Убедитесь, что сервер OKVpn появился в списке.
5. Выберите сервер и нажмите **Connect / Подключить**.

---

### Вариант B. Консольный клиент

1. Установите `xray`/`v2ray` (через репозиторий дистрибутива или официальный скрипт).
2. Сохраните выданный конфиг от OKVpn в файл `config.json` (формат зависит от того, как именно выдали конфигурацию).
3. Запустите клиент, указав путь к конфигурации, например:

```bash
xray -config config.json
```

4. Настройте перенаправление трафика (SOCKS/HTTP‑прокси, iptables, fwmark и т.п.) — детали обычно есть в расширенной инструкции OKVpn.

---

## 3. Проверка подключения

Проверьте:

1. Откройте браузер и зайдите на сайт, который был недоступен без VPN.
2. Посмотрите IP‑адрес на сервисе проверки IP.

Если всё работает — значит, конфигурация VLESS от OKVpn применена корректно.

---

## 4. Отладка и поддержка

Если:

- клиент не стартует;
- в логах ошибки;
- подключение есть, но сайты не открываются;

то:

1. Проверьте правильность времени/даты в системе.
2. Попробуйте другой сервер/страну.
3. Отключите сторонние файрволы/фильтры, которые могут блокировать трафик.

Если сами не хотите разбираться — напишите в поддержку **@okvpn_support_bot**, приложите:

- ваш дистрибутив и версию ядра;
- используемый клиент;
- кусок лога с моментом ошибки (без приватных данных).

---

[!button text=\"⬅️ Назад к VLESS\" variant=\"link\"](../)
